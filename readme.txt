1 = ofline event
2 = online event

Event Visibility
1 = public
2 = privet


1 = Single Event
2 = Recurring Event

1(on) || 0(off) = is_clock_countdown
1(on) || 0(off) = display_start_time
1(on) || 0(off) = display_end_time

0(deactive) || 1(active) || 2(reject) = status

raise tickets close status
0 = open | 1 = close